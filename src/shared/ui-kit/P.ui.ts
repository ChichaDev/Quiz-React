import styled from 'styled-components';

export const StyledP = styled.p`
  color: ${({ theme }) => theme.colors.basic2};
  font-size: 17px;
  font-weight: 400;
  line-height: 24px;
  text-align: center;
  margin: 0;
`;
